# 更新日志

## v0.4.5（2026-09-07）

完整发布说明、平台安装包对照与安装提示见 [`docs/releases/v0.4.5.md`](docs/releases/v0.4.5.md)。

### Agent 与 AI 对话

- Agent 提交任务后立即清空输入框；启动失败时保留原输入，便于重试。
- Claude 完全访问模式映射到 `bypassPermissions`，不再重复触发普通权限询问，越界访问和 `git push` 硬性拦截继续保留。
- Agent 与 AI 对话自动识别当前文档，显示可移除上下文标签，并在文档切换或编辑后同步最新内容。
- Agent 与 AI 生成内容均可插入编辑器，插入前自动添加 Markdown 分隔线 `---`。

### 编辑器与资源管理器

- 关闭 Monaco 单击后的同词高亮，避免未选中文字出现灰色高亮块，真实选区高亮保持不变。
- 修复删除工作区最后一个文件后资源管理器列表残留的问题。
- 删除已打开且有未保存修改的文档时进行明确提醒，确认后自动关闭编辑器并删除文件。

### 发布质量

- 版本源统一为 0.4.5。
- 增加 Agent、AI 上下文、编辑器高亮和文档插入行为的回归测试。

## v0.4.4（2026-09-06）

完整发布说明、平台安装包对照与安装提示见 [`docs/releases/v0.4.4.md`](docs/releases/v0.4.4.md)。

### 数据安全与编辑体验

- 修复标签页切换共用撤销栈的问题，每个标签保留独立 Monaco 模型和撤销状态。
- 防止 AI 异步结果回写到已切换或已变化的文档；AI 快照撤销不会覆盖后续手写内容。
- 修复另存为、更新安装、关闭标签和退出应用期间的异步竞态；保存失败会阻止关闭并给出提示。
- 实现自动保存，按编辑器设置周期保存已有路径且已修改的标签。
- 完善输入法引擎、快捷栏等编辑器设置在 Rust 与前端之间的持久化。

### 发布质量

- 版本源统一为 0.4.4，依赖更新到通过 npm 高危审计的版本。
- 扩充跨标签、AI 回写、保存队列、关闭保护和自动保存回归测试。

## v0.4.3（2026-09-06）

完整发布说明、平台安装包对照与安装提示见 [`docs/releases/v0.4.3.md`](docs/releases/v0.4.3.md)。

### 编辑器与输入法

- 修复 WebView2 生产包 CSP 拦截 Monaco 动态行样式，导致编辑期间已输入文字消失的问题。
- 修复首行上方字符闪现和折行行高归零，移除会遮盖 Monaco 原生布局的应用层遮罩。
- 原生 EditContext 默认启用，textarea 作为兼容回退；输入法引擎设置在 Rust 与前端之间完整保存并即时生效。
- 增加真实 WebView2 行几何、CSP 和组合输入回归验证，覆盖折行、取消、上屏、撤销、退格、中英混输和换段。

### 发布与质量

- 版本源统一为 0.4.3。
- GitHub Actions 质量门禁继续执行前端测试、Lint、生产构建、npm audit、Rust fmt、Clippy 和 Rust 测试，并发布 Windows、macOS、Linux 安装包。

## v0.4.1（2026-08-26）

完整说明见 [`docs/releases/v0.4.1.md`](docs/releases/v0.4.1.md)。

### 资源管理器

- **目录层级缩进**：一级、二级、三级目录在树中逐级缩进（基准 16px、每级 20px），不再与根目录标题对齐。
- **时间线标题单行**：「时间线」标签固定单行显示，过长的文件名以省略号截断，不再换行挤压计数徽标。
- **一级目录关闭按钮**：悬停工作区根目录显示 `×` 关闭按钮；右键菜单新增「关闭文件夹」。关闭后该目录从工作区移除，同时收起并清理其下所有已展开的子文件夹状态，并同步更新持久化的根目录列表（不删除磁盘内容）。
- **防误加根目录**：关闭某个工作区根后，30 秒内不会因当前打开文件位于其下而把它的子目录自动重新添加为独立根。
- **刷新保留展开内容**：自动刷新与手动刷新改为合并已懒加载的子目录内容，不再把已展开的二级、三级目录清空成「空文件夹」。

### 版本源对齐

- `package.json` 版本与其他版本源（`tauri.conf.json` / `Cargo.toml`）保持一致，发布合规测试同步更新。

## v0.4.0（2026-08-22）

完整说明见 [`docs/releases/v0.4.0.md`](docs/releases/v0.4.0.md)。

### 品牌与图标

- 全新蓝色渐变 `Z` 应用图标，替换旧图标并重新生成 Windows / macOS / iOS / Android / Store 全套平台资源（`npm run tauri icon`）。
- GitHub 首页 README 顶部插图替换为新的产品横幅（`docs/banner.png`）。

### 云同步面板（延续 v0.3.9 打磨）

- 修复状态栏弹窗对已配置 WebDAV / S3 误报「未启用」的问题。
- 云同步弹窗滑动开关改为与「设置」页一致的药丸形。
- 未配置时点击开关先关闭面板并跳转设置页；面板内「设置」按钮先关闭面板再跳转，避免遮罩遮挡。
- 历史版本支持「打开」（新标签页）与「另存为」，文档行双击打开最新版本。

### 视觉优化

- 面板与历史弹窗改用弹性入场动画；供应商卡片悬停上浮并加阴影、未启用降饱和、头部渐变。
- 同步中圆点叠加脉冲光晕；历史弹窗关闭按钮统一为 SVG 图标。

## v0.3.9（2026-08-21）

完整说明见 [`docs/releases/v0.3.9.md`](docs/releases/v0.3.9.md)。

### 云同步面板

- 修复状态栏弹窗对已配置 WebDAV / S3 误报「未启用」的问题（phase 归一化：已启用且后端 phase 为 `disabled` 时按 `idle` 处理）。
- 底部状态栏云同步弹窗的滑动开关改为与「设置」页一致的药丸形（提升选择器优先级规避全局 button 圆角覆盖）。
- 未配置 WebDAV / S3 时点击开关，先关闭面板并跳转设置页，不再在弹窗内显示「就绪」。
- 面板内「设置」按钮点击后先关闭面板，避免遮罩 `z-index` 挡住设置页导致无法点击。
- 历史版本列表支持「打开」（新标签页）与「另存为」，文档行双击打开最新版本。

### 视觉优化

- 面板与历史弹窗改用弹性入场动画；供应商卡片悬停上浮并加阴影，未启用卡片降饱和与透明度；卡片头部渐变背景。
- 同步中圆点叠加脉冲光晕；历史弹窗关闭按钮统一为 SVG 图标。

## v0.3.8（2026-08-17）

完整说明见 [`docs/releases/v0.3.8.md`](docs/releases/v0.3.8.md)。

### WebDAV / S3 备份

- 新增桌面版单向自动备份：本地保存成功后异步上传，保留最近 20 个不同内容版本。
- 支持 WebDAV 服务器与 S3 兼容对象存储（AWS S3、阿里云 OSS、腾讯云 COS、MinIO、Cloudflare R2 等），两者可同时启用、独立队列与状态。
- 支持在「设置 → WebDAV 备份」中配置服务器、用户名、密码 / 应用密码与远端根目录，并测试连接。
- 状态栏显示同步状态（未启用 / 等待同步 / 正在同步 / 已同步 / 同步失败），点击可浏览当前文档历史；错误状态支持点击重试。
- 离线或云端失败不影响本地保存；失败任务持久化并在启动或重试时恢复。
- 历史版本通过「另存为」下载，下载前校验 SHA-256。

### 主题

- 移除 Claude（Light/Dark）与 Notion（Light/Dark）四套主题，仅保留「深色主题」与「浅色主题」两套 VS Code 配色。
- 统一色彩饱和度与对比度，精简冗余颜色变量，优化语法高亮层级，确保编辑器各区域风格协调。

### 菜单栏

- 修复鼠标移出时菜单过快收回、二级子菜单难以触达的问题；收回延迟调整为 350ms 并加入透明桥接区域。

### 稳定性与质量

- 修复 PDF 导出浏览器池资源泄漏、文件命令授权根与符号链接遍历、并发下载竞态。
- 收紧 Tauri 内容安全策略（CSP）；修复 Agent 持久化节流、事件上限与流式输出格式等逻辑。

## v0.3.7（2026-08-17）

完整说明见 [`docs/releases/v0.3.7.md`](docs/releases/v0.3.7.md)。

### 文档转换

- 将可选转换模块升级至 v1.2.0，替换为 Firecrawl AnyDoc 0.1.9 原生 Rust 引擎。
- 新增 DOC、DOCM、PPT、PPS、POT、PPTM、PPSX、PPSM、XLSM、XLSB、ODF、RTF 和 EPUB 等格式入口。
- AnyDoc 通过统一文档模型生成结构化 Markdown，并在本地完成文本型 PDF 转换。
- 统一菜单、资源管理器、拖放和系统文件打开的格式路由，避免把二进制文档当作文本读取。

### 版本

- 应用版本统一更新为 0.3.7，转换模块版本统一更新为 1.2.0，产品名称统一为 Zeditor。

## v0.3.4（2026-07-22）

完整发布说明、平台安装包对照与安装提示见 [`docs/releases/v0.3.4.md`](docs/releases/v0.3.4.md)。

### 本地 Agent 与会话

- 新增 Claude Code、Codex 和 OpenCode 本地 Agent（Beta），复用各 CLI 登录状态与默认模型。
- 支持流式输出、命令审批、Git worktree 隔离、非 Git 目录会话授权和历史会话恢复。
- AI 对话与 Agent 均增加新建会话和历史记录入口，并修复工作目录路径差异导致的历史记录不可见。

### 编辑器与右键菜单

- 编辑器右键菜单新增 AI 润色、AI 翻译、复制为 HTML、导出 PDF/Word/HTML、插入图片和文件定位。
- “复制为”子菜单根据窗口边缘自适应展开方向，选中文本可直接调用现有 AI 翻译结果浮窗。
- 统一编辑器、预览区和资源管理器滚动条尺寸、圆角、显隐状态，移除多余概览标记。
- 编辑器当前行与预览语义块同步高亮；点击任一侧可将另一侧定位到对应内容并保持水平对齐。
- 引用、列表、表格等复合内容按完整区块高亮，避免只标记内部单行或段落。
- 修复安装版编辑器异常收窄和单击后误选文字，列表仅高亮直接内容，并完善滚动边界处的双向对齐。
- 为预览标题生成稳定锚点，支持 `#1`、中文章节编号和普通多语言标题的目录跳转。
- 关闭易混淆 Unicode 提示横幅、Monaco 粘性标题与动画滚动，消除顶部黑条并改善长文档滚动响应。

### 设置与界面

- 修正 Agent 设置在窄窗口下超出边界、对话错误提示重复和直接写入状态排版问题。
- 内容字号拖动时同步更新编辑器与预览区；保存设置先关闭窗口再异步持久化，减少卡顿。
- 压缩浮动编辑栏宽度，移除 Inkwell 明暗主题，并统一 AI 与 Agent 对话控件样式。

## v0.3.3（2026-07-21）

完整发布说明、平台安装包对照与安装提示见 [`docs/releases/v0.3.3.md`](docs/releases/v0.3.3.md)。

### 编辑器工具栏与快捷入口

- 固定编辑栏改为紧凑、主题自适应工具带，按编辑区宽度动态显示按钮，超出项折叠到“更多”菜单。
- 选中文本后显示浮动编辑栏，并修复部分图标在浅色主题下变黑、重复选区后浮动栏不再显示的问题。
- `/` 快捷命令面板补充常用编辑、插入、表格、代码、媒体、公式和 AI 写作入口。

### 搜索、本地化与预览同步

- 保留 Monaco Editor 原生搜索功能，搜索栏统一汉化为简体中文并适配当前主题。
- 修正编辑区与预览区文字基线、工具栏展开高度和预览区可视高度联动。
- 优化长文同步滚动锚点计算和底部滚动定位，减少浏览器预览中的卡顿感。

### AI 底部菜单

- AI 对话入口保留在左侧功能栏，底部 AI 菜单移除重复对话按钮。
- 校对文字独立列在底部菜单栏，可直接对选中文本或全文启动校对。
- 伴写功能独立显示开启状态，点击后可选择写作风格，AI 菜单中不再混放伴写设置。

## v0.3.2（2026-07-20）

完整发布说明、平台安装包对照与安装提示见 [`docs/releases/v0.3.2.md`](docs/releases/v0.3.2.md)。

### 界面多语言与编辑器菜单

- 新增简体中文、繁体中文和英文界面，首次启动时根据系统语言自动选择。
- 在“设置 → 外观”中新增界面语言选项，手动选择后持久保存。
- 将 Monaco 编辑器默认英文右键菜单替换为应用级多语言菜单，支持撤销、重做、剪切、复制、粘贴和全选。
- 统一修正主题、图床、搜索深度、帮助页等界面中遗留的英文操作文案。

### Markdown 快捷命令面板

- 在编辑器行首输入 `/`，弹出可搜索的 Markdown 快捷命令窗口。
- 支持标题、引用、无序/有序列表、任务列表、代码块、表格、图片、链接、公式、脚注、分割线和 Mermaid 流程图。
- 支持中文与英文筛选、鼠标点击、方向键、Enter/Tab 插入和 Esc 关闭。
- 面板会根据光标位置自动定位，并在窗口边缘限制高度与滚动范围。

### 发布与平台支持

- GitHub Actions 自动构建 Windows x64、macOS Intel、macOS Apple Silicon 和 Linux x64 安装包。
- 同步更新应用版本号、自动更新下载地址及 v0.3.2 平台安装包说明。
- 重新设计 Windows Markdown 关联文件图标，采用更简洁的文档轮廓与 Markdown 标识，并提供 16px 至 256px 七档 ICO 图层。
- 重新生成并替换 v0.3.2 各平台安装包，确保安装包包含最新图标与文档。
- 再次使用 GitHub Actions 覆盖生成 v0.3.2 各平台安装包，纳入多语言界面和中文右键菜单。

## v0.3.1（2026-07-20 更新）

完整发布说明、平台安装包对照与安装提示见 [`docs/releases/v0.3.1.md`](docs/releases/v0.3.1.md)。

### Windows 窗口交互修复

- 修复自定义标题栏偶发无法拖动的问题。
- 修复点击标题栏时窗口偶发最小化或窗口状态异常的问题。
- 修复窗口最小化后，点击 Windows 任务栏中的 Zeditor 图标偶发无法恢复界面的问题。
- 标题栏统一使用 Tauri 原生拖拽与双击处理，移除重复的手动窗口操作，避免 Windows 窗口消息竞态。
- 新增标题栏窗口交互回归测试。

## v0.3.1（2026-07-19）

- 升级文件管理器图标与常见文件类型识别。
- 优化 AI 对话面板、模型选择和窄侧栏布局。
- 改进批量替换、设置保存、文件扫描和图片上传的稳定性与性能。
- 加强 Markdown、AI 回复、搜索结果、附件与图片处理的安全防护。
- Windows 安装包增加 Markdown 文件关联、专用图标和离线文档转换器。
